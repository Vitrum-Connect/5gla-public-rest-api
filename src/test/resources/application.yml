app:
  version: '@project.version@'
  storage:
    sensor-master-data: ./sesnsor-master-data-test
  sensors:
    soilscout:
      url: https://soilscouts.fi/api/v1/
      username: ${SOILSCOUT_USERNAME}
      password: ${SOILSCOUT_PASSWORD}
fiware:
  context-broker-url: localhost:5661/
logging:
  level:
    root: info
    de.app.fivegla: trace